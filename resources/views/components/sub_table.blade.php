<div class="w-100 overflow-auto" style="font-size: 0.68rem; color: var(--text-color, #FFF) !important">
    <table class="w-100 {{$class ?? ""}}" id="{{ $id ?? "getXmlData" }}">
        <thead style="background: linear-gradient(to right, var(--primary-color, #1E3C72), var(--secondary-color, #33336F)); color: var(--text-color, #FFF); font-family: var(--heading-font, Inter, sans-serif);">
            <tr>
                {{ $td }}
            </tr>
        </thead>
        
    </table>
</div>